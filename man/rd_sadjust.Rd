% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sadjust.R
\name{rd_sadjust}
\alias{rd_sadjust}
\title{Within-period composition-adjusted RD estimator (Proposition \code{sdisc})}
\usage{
rd_sadjust(
  data,
  y,
  x,
  time,
  id,
  comparisons,
  t_rd,
  c = 0,
  h = NULL,
  bwselect = "cct",
  kernel = "triangular",
  se = c("bootstrap", "none"),
  B = 499L
)
}
\arguments{
\item{data}{long data frame, one row per unit-period.}

\item{y, x, time, id}{column names (strings) for outcome, running variable,
period, and unit id.}

\item{comparisons}{comparison-period values of \code{time}; the pair \verb{\{t0, t_rd\}}
is formed for each.}

\item{t_rd}{RD-period value of \code{time}.}

\item{c}{cutoff (default 0).}

\item{h}{optional fixed numeric bandwidth used for every fit; if \code{NULL}
(default) a per-(period, type, fit) CCT bandwidth is selected.}

\item{bwselect}{bandwidth selector label when \code{h} is \code{NULL} (default
\code{"cct"}); retained for forward compatibility.}

\item{kernel}{\code{"triangular"} (default), \code{"epanechnikov"}, or \code{"uniform"}.}

\item{se}{\code{"bootstrap"} (default) or \code{"none"}.}

\item{B}{bootstrap replications (default 499).}
}
\value{
An object of class \code{"rd_sadjust"}: a list with a tidy per-period
\code{table} (columns \code{comparison}, \code{period}, \code{role}, \code{S}, \code{S_bc}, \code{S_se},
\code{S_bc_se}, \code{D}, \code{D_bc}, \code{adjusted}, \code{adjusted_bc}, \code{n},
\code{n_control_switchers}) and meta fields.
}
\description{
Estimates the within-period composition term \eqn{S_t} that contaminates the
observed period-\eqn{t} discontinuity \eqn{D_t} when units sort across the
cutoff between regimes, and reports the composition-free discontinuity
\eqn{D_t - S_t}. With binary types (the unit's side of the cutoff in the
partner period) the term collapses to
\eqn{S_t = \Delta\pi_{above}\,(\mu_{above}^{(-)} - \mu_{below}^{(-)})},
where \eqn{\Delta\pi_{above}} is the cutoff jump in the above-type share and
\eqn{\mu_a^{(-)}} is the below-cutoff (control-side) outcome limit among
type-\eqn{a} units. Because \eqn{\mu_a^{(-)}} uses the control side it is
observed in every period, so \eqn{S_t} is estimable even at the RD period.
}
\details{
Uses the pairwise (P = 2) design: for each comparison period in
\code{comparisons}, the pair \verb{\{t0, t_rd\}} yields \eqn{S_{t0}} (type = the unit's
\code{t_rd} side) and \eqn{S_{t\_rd}} (type = the unit's \code{t0} side). Standard
errors are a unit-level cluster bootstrap at bandwidths held fixed at the
point-estimate values.
}
